import { defineConfig } from 'astro/config';

// `site` should be set to the final domain before launch (see PROJECT_BRIEF.md
// section 12 - no public site name has been chosen yet).
export default defineConfig({
  trailingSlash: 'always',
});
